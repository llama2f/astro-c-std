---
import { Image } from 'astro:assets'

import CardInner from '@components/module/CardInner.astro'

const {src,alt} = Astro.props
---
    <CardInner style="max-w-60 w-full md:w-5/12">
      <Image src={src} alt={alt} />
    </CardInner>
 
